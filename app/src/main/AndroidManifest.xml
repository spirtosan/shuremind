<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- D-08/D-17/Session-1-addendum: SCHEDULE_EXACT_ALARM (opt-in, user-facing toggle), not
         USE_EXACT_ALARM, to keep Play Store distribution open. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- D-42: full-screen intent for the alarm-mode ring screen. Sideload-fine; Play review is a
         later concern (Session 1 notes) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:name=".ShuRemindApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.ShuRemind">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ShuRemind">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- D-42: alarm-mode ring screen, launched only via the alarm notification's full-screen intent. -->
        <activity
            android:name=".ui.AlarmRingActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.ShuRemind" />

        <!-- D-18: persists the user's picked app language across process death (autoStoreLocales). -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!-- M3 (D-07): single-next-alarm target. Broadcast-only, never launched by other apps. -->
        <receiver
            android:name=".system.AlarmReceiver"
            android:exported="false" />

        <!-- M3 (D-06/D-07/D-10/D-17): recompute + missed-fire recovery + re-arm. No directBoot handling. -->
        <receiver
            android:name=".system.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- M3 STEP 6: Done/Snooze/Skip notification actions. -->
        <receiver
            android:name=".system.NotificationActionReceiver"
            android:exported="false" />

        <!-- D-42: auto-silences an alarm-mode ring ~5 minutes after it posts. -->
        <receiver
            android:name=".system.AlarmSilenceReceiver"
            android:exported="false" />
    </application>

</manifest>
